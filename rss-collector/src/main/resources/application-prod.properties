# DataSource
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://${RDS_ENDPOINT}:5432/newstagram
spring.datasource.username=${RDS_USERNAME}
spring.datasource.password=${RDS_PASSWORD}

# Redis
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=${REDIS_PORT}
spring.data.redis.ssl.enabled=true
# spring.data.redis.password=${REDIS_PASSWORD}

# SQL Initialization
spring.sql.init.mode=never
# spring.sql.init.schema-locations=classpath:sql/00_init.sql
# spring.sql.init.data-locations=classpath:sql/01_AddData.sql
spring.jpa.defer-datasource-initialization=false
spring.jpa.hibernate.ddl-auto=update

# Server CORS Setting
server.forward-headers-strategy=native

# SQL TIMEZONE Setup
spring.datasource.hikari.connection-init-sql=SET TIME ZONE 'Asia/Seoul';
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Seoul
spring.jackson.time-zone=Asia/Seoul